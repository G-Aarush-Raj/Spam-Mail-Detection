# Spam-Mail-Detection
A spam mail detection system using a Naive Bayes model that classifies emails as spam or ham based on word frequency analysis. It employs Variable Elimination for probabilistic inference and tests input emails against the trained model.
